# Dataset files

Place the extracted Olist CSV files here for local development only. They are ignored by
Git because they are source data, not application code.

The seed command requires:

- `olist_customers_dataset.csv`
- `olist_products_dataset.csv`
- `olist_orders_dataset.csv`
- `olist_order_items_dataset.csv`
